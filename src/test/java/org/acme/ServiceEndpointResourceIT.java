package org.acme;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class ServiceEndpointResourceIT extends ServiceEndpointResourceTest {

    // Execute the same tests but in native mode.
}